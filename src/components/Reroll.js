import React from 'react';
import '../App.css';

class Reroll extends React.PureComponent {
    render() {
      return (
        <div>
          <h1>Reroll Page</h1>
        </div>
      );
    }
  }

export default Reroll;
